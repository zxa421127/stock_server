# 安全事件响应

发现异常登录、Token滥用、数据库泄露或服务器入侵时：

1. 保留日志和磁盘快照，不先清理证据。
2. 从负载均衡或防火墙隔离受影响实例。
3. 吊销可疑API Token和管理员会话。
4. 轮换外部Provider、飞书、管理员、Session和审计密钥。
5. 检查管理员操作历史、API访问历史、Nginx日志、systemd日志和数据库变更。
6. 从可信构建产物重建服务器，不在疑似被入侵主机上“就地修复”。
7. 恢复后执行依赖审计、发布包秘密扫描和端到端验收。
8. 记录时间线、根因、受影响范围和长期修复项。
