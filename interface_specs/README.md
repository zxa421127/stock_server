# 市场接口规格目录

运行时读取 `current.json` 指向的不可变发布版本。首次启动时会根据项目140接口目录生成seed版本；管理员在“规格变化中心”同步Tushare官网后，候选规格必须人工确认并发布，绝不会自动覆盖正式版本。

- `releases/`：不可变正式规格版本；
- `candidates/`：官网同步生成的候选版本和差异；
- `source/`：同步工具保存的官方原始规格；
- `current.json`：当前正式版本指针。
